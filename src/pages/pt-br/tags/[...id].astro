---
import TagEntry from "@components/Tags/TagEntry.astro";
import { getTagPaths } from "@lib/utils";

export async function getStaticPaths() {
  return await getTagPaths("pt-br");
}

const { id } = Astro.params;
const { posts } = Astro.props;
---

<TagEntry id={id} posts={posts} />